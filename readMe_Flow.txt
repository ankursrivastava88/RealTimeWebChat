> created the package.json file here, with all node dependencies.
	> using command prompt, navigate to this folder and type command "npm install", which then installs all dependencies in the "node_modules" folder in this dir.
	
> create index.js, then "node index.js", the server is ready and listening for req on port=3700
	> open url: http://127.0.0.1:3700/ 
	
> creating template dir "tpl", for jade files(to give HTML structure)
> create public folder for static js and image files